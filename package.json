{
  "name": "automation-workflow-builder",
  "version": "1.0.0",
  "private": true,
  "description": "n8n-style workflow builder and executor",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:shared && npm run build:client && npm run build:server && npm run copy:client",
    "build:shared": "cd shared && npm run build",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "copy:client": "cp -r client/dist server/dist/server/src/public",
    "start": "cd server && npm start",
    "lint": "concurrently \"cd client && npm run lint\" \"cd server && npm run lint\"",
    "install:all": "npm install && cd shared && npm install && cd ../server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
